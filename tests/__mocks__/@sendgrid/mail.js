const setApiKey = (key) => {
    // console.log(`Mocked mail key set ${key}`);
};

const send = (mail, a, b) => {
    // console.log(`Mocked mail sent to ${mail.to} with text "${mail.text}"`);
}

module.exports = {
    setApiKey,
    send
}